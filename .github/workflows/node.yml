name: Node.js CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        cache-dependency-path: dreamai-clone/frontend/package-lock.json

    - name: Install Dependencies
      working-directory: dreamai-clone/frontend
      run: npm ci

    - name: Build
      working-directory: dreamai-clone/frontend
      run: npm run build --if-present

    - name: Run Tests
      working-directory: dreamai-clone/frontend
      run: npm test
