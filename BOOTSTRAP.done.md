# BOOTSTRAP.md - ADP v4.0 Environment Initializer

This file is automatically generated by the ADP v4.0 Engineer.
It ensures the development environment is correctly set up.

## Instructions (Self-Executing)

When read, execute the following steps:

1.  **Check Environment:**
    - Ensure `src/` and `tests/` directories exist.
    - Ensure a `.gitignore` exists (ignoring `__pycache__`, `.env`, `node_modules`).
    - Check if `git` is initialized (if applicable/safe).

2.  **Verify Setup:**
    - Create a simple test file `tests/test_setup.py` that imports `sys` and prints "Environment OK".
    - Run `python3 tests/test_setup.py`.

3.  **Report:**
    - If successful, delete this file or rename to `BOOTSTRAP.done.md`.
    - If failed, log the error to `docs/LOG.md` and ask for user intervention.

## Why this exists?

To guarantee a consistent, reproducible development environment for the ADP v4.0 agent.
